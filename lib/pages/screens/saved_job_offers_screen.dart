import 'package:flutter/material.dart';

class SavedJobOffersScreen extends StatelessWidget {
  const SavedJobOffersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Salvati"),
      ),
    );
  }
}
